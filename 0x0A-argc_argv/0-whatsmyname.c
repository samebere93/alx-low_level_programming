#include <stdio.h>

/**
 * main - Function that print a program name
 * @argc: argument count
 * @argv: arguments vector
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
